fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
zeep>=4.2
cryptography>=42.0
bcrypt>=4.1  # hashing de contraseñas (login de contadores)
pyjwt>=2.8  # tokens de sesión (JWT)
email-validator>=2.1  # validación de emails (pydantic EmailStr)
requests>=2.31
apscheduler>=3.10
reportlab>=4.1  # generación del PDF del comprobante emitido (representación impresa)
pdfplumber>=0.11  # lectura del PDF de las Liquidaciones del agro (Importe Bruto por coordenadas)
segno>=1.6  # código QR de ARCA en el comprobante (RG 4291); wheel puro, sin PIL
psycopg2-binary>=2.9  # driver Postgres (producción; en local se usa SQLite por defecto)
patchright  # Playwright stealth + headless real (listar representados)
cloakbrowser  # Chromium stealth con humanize (bootstrap del cert: maneja el árbol JS animado de ARCA)
