node_modules/
.env
.env.local
.env.*.local
dist/
build/
.DS_Store
*.log
.vscode/
.idea/
.claude/
*.tsbuildinfo

# Config del MCP ssh-vps (credenciales/host del VPS) — NUNCA al repo
.mcp.json

# Notas locales de trabajo / contexto y reportes sensibles — NO al repo
CONTEXTO-*.md
PENTEST-*.md

# Páginas guardadas / dumps de research y debug (pueden contener datos reales) — NO al repo
ARCA _ Inicio.html
categorias.html
serviciosarca.html
serviciosarca2.html
*_files/

# Spikes de exploración que NO son parte del sistema (su lógica ya vive en el motor) — quedan local
backend/explorar_saldos_ccma.py
backend/explorar_p08_montos.py
