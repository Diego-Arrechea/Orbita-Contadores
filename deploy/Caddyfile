# Caddy proxea /api -> uvicorn (FastAPI) con HTTPS automatico (Let's Encrypt).
# El frontend NO se sirve aca: vive en Vercel (app.tudominio.com).
# Reemplaza api.tudominio.com por tu subdominio real.

api.tudominio.com {
	encode gzip
	reverse_proxy 127.0.0.1:8000
}
