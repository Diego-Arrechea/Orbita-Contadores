# Caddy proxea todo el trafico -> uvicorn (FastAPI) con HTTPS automatico (Let's Encrypt).
# El frontend vive en Vercel (contadores.orbitaglobalmarketing.com).

api.contadores.orbitaglobalmarketing.com {
	encode gzip
	reverse_proxy 127.0.0.1:8000
}
